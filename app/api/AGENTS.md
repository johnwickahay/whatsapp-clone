# API Directory - AGENTS.md

This directory contains the Next.js API route handlers.

Currently, most database logic is handled directly via the Supabase client in the frontend or via RPCs, but specific API routes can be added here for backend-only logic.
